apiVersion: v1
kind: runners
metadata:
  name: "example-runner-yaml"
  labels: 
    os: "linux"
    cloud_self: aws
spec:
  active: true
  description: "A runner resource"
  requirements:
    matchLabels:
      cloud: "wha?"
